import { ActivemenuDirective } from './activemenu.directive';

describe('ActivemenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivemenuDirective();
    expect(directive).toBeTruthy();
  });
});
